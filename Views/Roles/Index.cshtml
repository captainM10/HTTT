@model IEnumerable<CanhGac.Models.Role>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h1>Quyền truy cập</h1>
<p>
    <div class="col-lg-13 text-right">
        <a class="btn btn-primary" asp-action="Create"><i class="anticon anticon-plus-circle m-r-5"></i> <span>Thêm quyền truy cập</span></a>
    </div>
</p>
<table class="table">
    <thead>
        <tr  class="text-center">
            <th>
                #
            </th>
            <th>
                Tên quyền truy cập
            </th>
            <th>Thao tác</th>
        </t>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td class="text-center">
                    @Html.DisplayFor(modelItem => item.RoleId)
                </td  class="text-center">
                <td  class="text-center">
                    @Html.DisplayFor(modelItem => item.RoleName)
                </td>
                <td  class="text-center">
                    <a asp-action="Edit" asp-route-id="@item.RoleId" class="btn btn-success btn-sm">Chỉnh sửa</a>
                    <a asp-action="Details" asp-route-id="@item.RoleId" class="btn btn-info btn-sm">Xem chi tiết</a>
                    <a asp-action="Delete" asp-route-id="@item.RoleId" class="btn btn-danger btn-sm">Xóa</a>

                </td>
            </tr>
        }
    </tbody>
</table>
